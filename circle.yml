machine:
  java:
    version: oraclejdk8
  services:
    - docker

compile:
  override:
    - ./gradlew --stacktrace clean distTar -x test
    - ls -al
    - ls -al build/distributions
    - bash ./build_docker.sh
test:
  override:
    - echo "ignore test..."
