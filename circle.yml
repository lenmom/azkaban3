machine:
  java:
    version: oraclejdk8
  services:
    - docker

compile:
  override:
    - ./gradlew --stacktrace clean distTar -x test
    - bash ./build_docker.sh
test:
  override:
    - echo "ignore test..."
